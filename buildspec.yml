version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto17
  build:
    commands:
      - mvn clean install
      - mv target/*.war app.war
artifacts:
  files:
    - app.war
#artifacts:
#  files:
#    - target/*.war